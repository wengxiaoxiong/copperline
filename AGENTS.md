# Copperline 开发约定

## 项目定位与入口

这是浏览器单人沙盒原型，技术栈为 TypeScript、Three.js、Rapier WASM、Vite 和原生 HTML/CSS。包名包含 react，但项目没有 React。保持现有技术栈；新增框架或依赖必须有实际需求。

开始改动前阅读 [架构文档](docs/ARCHITECTURE.md)、[开发验证说明](docs/DEVELOPMENT.md)，检查 `git status` 和相关源码。未提交改动属于当前工作基线，保留已有功能与无关 diff，不执行 reset/clean 来获得干净目录。

## 模块职责

- `main.ts`：浏览器启动、Rapier 初始化和失败提示。
- `game.ts`：会话与模式、输入、移动和玩法结果编排。新增可独立计算或呈现的逻辑应放到对应模块，不持续扩大 Game。
- `generation.ts`：确定性世界规划与布局，不引入 DOM、Three.js 或 Rapier。
- `world.ts` / `landscape.ts`：静态世界、加载/卸载、模型与碰撞。
- `population.ts`：动态角色身份、NPC、车辆控制权与掉落。
- `inventory.ts` / `combat.ts`：拥有关系、购买规则、武器时序。
- `targeting.ts`：相机瞄准与枪口遮挡计算，返回命中结果，不扣血、不播放音效。
- `hud.ts` / `equipment-panel.ts`：呈现数据到 DOM，不修改玩法状态、不持有 Game。
- `camera-rig.ts` / `atlas.ts`：相机姿态与地图交互；世界查询仍使用共享 WorldPlan。

模块之间传递所需数据，不用整个 Game 对象作为通用上下文。保持职责清晰，不为缩短文件而增加转发层。新增代码沿用相邻模块风格，避免无关的全库格式化。

## 必须保持的行为

1. `logical = local + city.offset`。地图、世界规划、区块编号使用逻辑坐标，渲染和物理使用局部坐标。负区块编号使用 `chunkAt()`，不可截断取整。
2. 新世界对象同时考虑创建、卸载、原点平移和 reset。共享几何/材质不能随单一区块释放；独占资源需明确回收。
3. 地形、桥梁、车流和导航共用 WorldPlan。改路网时同时检查视觉与碰撞，不能只验证地图路径。
4. 恢复游戏必须等待 Pointer Lock 实际成功；失败允许重试。地图、装备、暂停不得保留移动或射击输入，也不得继续推进玩法。
5. 车辆接管保留同一身份和刚体；不要重新生成一辆外观相似的车代替接管。
6. 射击先从相机确定瞄准点，再从枪口检查遮挡，不能穿过枪口前的墙。射线不可修改相机位置。
7. 购买只扣一次款；切枪保留各自弹药与换弹进度。HUD 与装备渲染没有购买或装备副作用。
8. 生成可复现不代表整局模拟确定性。当前没有跨刷新存档；区块数固定不代表会话总内存恒定。

## 验证与交付

```sh
npm test
npm run build
```

测试使用 Node test runner + tsx；构建包含 src 的 strict TypeScript 检查。优先通过真实模块接口测行为，浏览器 DOM 等无法直接运行的依赖可替换，不能复制业务逻辑来验证复制品。

输入、相机或界面改动需要明确报告浏览器验证范围。单元测试、脚本摆放场景、构建通过都不等于完整键鼠游玩或帧率验收。`window.__game` 仅用于开发；场景脚本会修改当前游戏状态。

修改架构时同步 docs，中文说明具体改动及验证结果。提交时只纳入授权范围；提交、推送、部署分别报告实际状态。第三方代码的署名与许可证必须保留，详见 `THIRD_PARTY_NOTICES.md` 和 `docs/LEGAL.md`。
